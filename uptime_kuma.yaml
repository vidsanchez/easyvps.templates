metadata:
  name: Uptime_kuma
  description: Plantilla para Uptime_kuma
  version: 1.0.0
  category: General
  dependencies: []
  required_networks:
  - network_public
  user_fields:
  - field: subdominio
    label: Subdominio
    type: string
    required: true
    default: ''
    description: Configuración para subdominio
    validation:
      pattern: ^[a-z0-9-]+$
      min_length: null
      max_length: null
  tags:
  - uptime_kuma
template:
  version: '3.7'
  services:
    uptime_kuma:
      image: luizeof/smtp2http:latest
      entrypoint:
      - smtp2http
      - --user=[[username]]
      - --pass=[[password]]
      - --webhook=[[subdominio]]
      networks:
      - network_public
      deploy:
        placement:
          constraints:
          - node.role == manager
  networks:
    network_public:
      external: true
      name: network_public
